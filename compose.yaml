services:
  app:
    build:
      context: .

    working_dir: /workspace

    volumes:
      - .:/workspace
      - node_modules:/workspace/node_modules

    ports:
      - "8081:8081"

    stdin_open: true
    tty: true
    init: true

    command: sleep infinity

volumes:
  node_modules:
